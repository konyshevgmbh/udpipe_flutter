name: udpipe_flutter_example
description: Demo app for the udpipe_flutter package — NLP tokenisation, POS tagging and dependency parsing.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.4.0

dependencies:
  flutter:
    sdk: flutter
  udpipe_flutter:
    path: ../

flutter:
  uses-material-design: true
  assets:
    - assets/models/
